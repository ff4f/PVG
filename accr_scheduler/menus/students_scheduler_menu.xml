<?xml version="1.0"?>
<odoo>
    <!-- Student Scheduler App Menu -->
    <menuitem id="menu_students_scheduler" name="Student Scheduler" />
    <!-- Action to open the accr_timing -->
    <act_window id="action_accr_timing" name="Task Timing" res_model="accr.timing" view_mode="tree,form" />
    <!-- Menu item to open the accr_timing -->
    <menuitem id="menu_accr_timing" name="Task Timing" parent="menu_students_scheduler" action="action_accr_timing" />
    <!-- Action to open the accr_session -->
    <act_window id="action_accr_session" name="Task session" res_model="accr.session" view_mode="tree,form" />
    <!-- Menu item to open the accr_session -->
    <menuitem id="menu_accr_session" name="Task session" parent="menu_students_scheduler" action="action_accr_session" />
    <!-- Action to open the accr_generate_timetable -->
    <act_window id="action_accr_generate_timetable" name="Generate Time Table" res_model="accr.generate.time.table" view_mode="tree,form" />
    <!-- Menu item to open the accr_session -->
    <menuitem id="menu_accr_generate_timetable" name="Generate Time Table" parent="menu_students_scheduler" action="action_accr_generate_timetable" />

</odoo>